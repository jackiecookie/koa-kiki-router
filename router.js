var path = require('path');
var fs = require('fs');
const debug = require('debug')('koa-kiki-router');


function Router(app, option) {

}

/*
 * find all files from path
 * @dir {string} path dir
 * */
Router.prototype._findFiles = function (dir) {

}